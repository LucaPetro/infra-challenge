{
  "name": "challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy-infra": "pnpm --filter ./infra exec cdk deploy --all --require-approval never --context stage=dev",
    "deploy-inventory": "pnpm --filter ./inventory exec serverless deploy",
    "deploy-all": "pnpm run deploy-infra && pnpm run deploy-inventory"  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.29.1",
  "devDependencies": {
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.985.0",
    "esbuild-plugin-tsc": "^0.5.0",
    "serverless-scriptable-plugin": "^1.3.1"
  }
}
